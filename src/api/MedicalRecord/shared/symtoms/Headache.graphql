# 두통
type Headache {
    id: Int!
    headache_headache: Boolean! #두통
    headache_migraine: Boolean! #편두통
    createdAt: String!
    updatedAt: String
    medicalRecordId: Int
    medicalRecord: MedicalRecord
}