type UpdateMyPasswordResponse {
    ok: Boolean!
    error: String
}

type Mutation {
    UpdateMyPassword(
        currentPassword: String!
        newPassword: String!
    ): UpdateMyPasswordResponse!
}