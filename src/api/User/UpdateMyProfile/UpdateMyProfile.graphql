type UpdateMyProfileResponse {
    ok: Boolean!
    error: String
    user: User
}
type Mutation {
    UpdateMyProfile(
        isAbroad: Boolean
        firstName: String
        lastName: String
        age: Int
        password: String
        address: String
        gender: Gender
        nationality: Nationality
    ): UpdateMyProfileResponse!
}