type CreateBoardResponse {
    ok: Boolean!
    error: String
    boardId: Int
}

type Mutation {
    CreateBoard(    
        text: String!
        selector: SELECTOR!
    ): CreateBoardResponse!
}